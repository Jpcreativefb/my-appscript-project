/* =========================
   LEGACY APP FILE

   App configuration, sheet helpers,
   and profile logic were moved to:

   backend/core/AppConfig.js
   backend/core/SheetHelpers.js
   backend/engines/ProfileEngine.js

   Apps Script server-rendered frontend
   helpers were removed because the
   frontend now runs on Cloudflare Pages.
========================= */